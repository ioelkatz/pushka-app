<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ID de publicidad: lo inyecta Firebase Analytics al fusionar manifests,
         no lo pedimos nosotros. La app no muestra anuncios, no hace campañas
         y en Seguridad de los datos declaramos que no hay publicidad ni
         marketing. Recopilar el identificador seria juntar un dato que no
         le sirve a nadie, y ademas obliga a declarar en Play Console que la
         app usa ID de publicidad. Analytics sigue funcionando sin el; solo
         pierde la atribucion de campañas, que no usamos. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />

    <!-- READ_MEDIA_IMAGES se retiro: image_picker usa el Photo Picker de
         Android en 13+, que no necesita ningun permiso porque el sistema
         entrega solo la foto elegida. El permiso daba acceso a TODA la
         galeria para subir un avatar, y encima obliga a justificarlo en la
         declaracion "Permisos de fotos y videos" de Play Console.
         READ_EXTERNAL_STORAGE queda acotado a Android 12 y anteriores, donde
         el plugin cae al selector clasico. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep links: pushka:///, /history, /reminders, /settings,
                 /prayers, /support, /about, /join/:slug. Whitelist enforced
                 client-side in DeepLinkService. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="pushka"/>
            </intent-filter>
            <!-- HTTPS App Links: all trusted hosts must autoVerify to true.
                 Round-5 audit HIGH fix: only pushka-app-ioel.web.app was
                 verified; the other origins bounced to a chooser dialog
                 ("Open with browser or app?") instead of direct-to-app.

                 2026-09-03: se cambiaron los tres hosts de pushkapp.cc por
                 app.jabadencampus.com. pushkapp.cc NUNCA se compro — el plan
                 cambio a un subdominio del cliente — asi que la app declaraba
                 tres dominios inexistentes y NO declaraba el unico que se usa
                 de verdad. Los links de invitacion abrian el navegador.

                 Cada host de aca tiene que servir /.well-known/assetlinks.json
                 con el SHA-256 de Play App Signing, y tiene que coincidir con
                 _trustedHttpsHosts de lib/core/deep_link_handler.dart. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="pushka-app-ioel.web.app" android:pathPrefix="/join/"/>
                <data android:scheme="https" android:host="pushka-pwa.web.app" android:pathPrefix="/join/"/>
                <data android:scheme="https" android:host="pushka-pwa.firebaseapp.com" android:pathPrefix="/join/"/>
                <data android:scheme="https" android:host="app.jabadencampus.com" android:pathPrefix="/join/"/>
                <data android:scheme="https" android:host="www.app.jabadencampus.com" android:pathPrefix="/join/"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Default FCM notification channel + icon. When the app is killed or
             backgrounded, Firebase Messaging builds the system notification
             itself instead of routing through FlutterLocalNotifications — so
             it needs the channel id embedded in the manifest. Without this
             meta-data, killed-state FCM lands in the low-importance
             "Miscellaneous" fallback channel (silent + collapsed on One UI),
             defeating our pre-created high-importance `pushka_notifications`
             channel.
             TODO: replace @mipmap/ic_launcher with @drawable/ic_notification
             (monochrome Pushka silhouette) so the status-bar glyph doesn't
             render as a generic white blob. Same for a @color/notification_color
             resource — skipped until the asset lands. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="pushka_notifications" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <!-- flutter_local_notifications v17+ requires these receivers to be
             declared explicitly. Without them, AlarmManager fires the
             scheduled broadcast but no receiver is registered, so the
             notification never appears (silent failure). -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
