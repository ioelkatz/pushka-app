<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Universal Links — cada dominio de esta lista tiene que servir
	     /.well-known/apple-app-site-association con NUESTRO Team ID. Los dos
	     de abajo tienen que servirlo. VERIFICAR EL CONTENIDO, no el codigo
	     HTTP: Firebase Hosting devuelve 200 con un AASA VACIO
	     ({"applinks":{"apps":[],"details":[]}}) cuando el sitio no tiene el
	     archivo, y eso rompe los deep links sin dar ningun error. Asi estaba
	     app.jabadencampus.com hasta el 2026-09-24, cuando se agrego el archivo
	     en web/.well-known/. Los dos siguen inservibles hasta que el AASA
	     lleve el Team ID real, que llega con el alta de Apple.

	     ⚠️ Aca figuraban pushkaapp.com y www.pushkaapp.com. Ese dominio NO es
	     nuestro: redirige a pushkahub.com, el producto de otra empresa. iOS
	     habria ido a buscar el AASA ahi, no lo habria encontrado con nuestro
	     Team ID, y los deep links habrian fallado en silencio. Se sacaron el
	     2026-09-24. El dominio propio es jabadencampus.com. -->
	<key>com.apple.developer.associated-domains</key>
	<array>
		<string>applinks:app.jabadencampus.com</string>
		<string>applinks:pushka-app-ioel.web.app</string>
	</array>

	<!-- Apple Pay merchant ID. Must be created in Apple Developer Portal
	     (Identifiers → Merchant IDs) before the build will sign. The value
	     here is also passed to Stripe via the MERCHANT_IDENTIFIER dart-define
	     so flutter_stripe activates the wallet sheet. -->
	<key>com.apple.developer.in-app-payments</key>
	<array>
		<string>merchant.com.pushka.app</string>
	</array>

	<!-- Push Notifications. `production` covers TestFlight + App Store
	     builds. Debug builds run via Xcode use `development` automatically
	     when CODE_SIGN_STYLE is Automatic. For Codemagic-managed signing
	     this stays `production`. -->
	<key>aps-environment</key>
	<string>production</string>

	<!-- App Attest — backs Firebase App Check on iOS 14+. `production`
	     for distributed builds; iOS auto-falls-back to DeviceCheck on older
	     devices. Sandbox-only attestation runs when `development` is set,
	     which we don't ship. -->
	<key>com.apple.developer.devicecheck.appattest-environment</key>
	<string>production</string>

	<!-- Sign in with Apple. Required by App Store review (4.8 Login
	     Services) any time we offer Google or Facebook sign-in. The package
	     `sign_in_with_apple` is in pubspec — this entitlement turns it on. -->
	<key>com.apple.developer.applesignin</key>
	<array>
		<string>Default</string>
	</array>
</dict>
</plist>
